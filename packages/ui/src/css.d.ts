// Ambient declarations for style/asset side-effect imports (Tailwind entry + @fontsource CSS).
// These are handled by the consumer's bundler (Vite); TypeScript only needs them to type-resolve.
declare module "*.css";
