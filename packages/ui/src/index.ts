// Public API for @erp/ui — the owned Radix + token component layer.
// Foundation utilities land here now; primitives (Button, Input, InkChip, …) arrive in Task 3.

export { cn, cva, type VariantProps } from "./lib/cn.js";
