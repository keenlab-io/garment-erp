# The apps never call the k8s API, so no RBAC (Role/RoleBinding) is attached — this
# ServiceAccount exists solely to carry the ghcr.io image pull secret.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: erp-app
  namespace: erp
imagePullSecrets:
  - name: ghcr-pull
