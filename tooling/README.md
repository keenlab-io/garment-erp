# tooling/

Placeholder for repo scripts: DB migrate, codegen, seed (spec §3).

Nothing here yet — `pnpm db:migrate` currently routes to a stub in `apps/api`.
Add real migration/seed tooling here when an ORM is chosen.
